package com.acss.smg.service.impl;import com.acss.smg.dao.impl.UserDAOImplement;import com.acss.smg.model.UserModel;import com.acss.smg.service.UserLoginService;/*****************************************************/import java.io.IOException;public class UserLoginImplement implements UserLoginService {	// Constructor 	private UserDAOImplement userAccess = new UserDAOImplement();	// Variable	private long accountNumber;	private String password;	private boolean isValidUser;		public boolean validateUser(UserModel user) throws IOException {		try {			UserModel userLogin = userAccess.retrieveData(user);			user.setCustomerID(userLogin.getCustomerID());			user.setAccountType(userLogin.getAccountType());						if (user.getAccountNo() == userLogin.getAccountNo() && 				user.getPassword().equals(userLogin.getPassword())) {				isValidUser = true;			}					} catch (IOException e) {			e.printStackTrace(); 			System.err.println(e.getMessage()); 		}		return isValidUser;	}}